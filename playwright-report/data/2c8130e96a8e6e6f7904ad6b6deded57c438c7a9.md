# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: admin-messages.spec.ts >> Admin messages management >> messages list or empty state is visible
- Location: e2e\admin-messages.spec.ts:18:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('heading', { name: /message/i })
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for getByRole('heading', { name: /message/i })

```

```yaml
- heading "Sign in to build your portfolio" [level=1]
- paragraph: Welcome back! Please sign in to continue
- button "Sign in with Google Continue with Google":
  - img "Sign in with Google"
  - text: Continue with Google
- paragraph: or
- text: Email address
- textbox "Email address":
  - /placeholder: Enter your email address
- text: Password
- textbox "Password":
  - /placeholder: Enter your password
- button "Show password":
  - img
- button "Continue":
  - text: Continue
  - img
- text: Don’t have an account?
- link "Sign up":
  - /url: https://nearby-koi-38.accounts.dev/sign-up?__clerk_db_jwt=dvb_3EE4xp7jk4tTTLmQmQucga0Fg60
- paragraph: Secured by
- link "Clerk logo":
  - /url: https://go.clerk.com/components
  - img
- paragraph: Development mode
- region "Notifications (F8)":
  - list
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | 
  3  | test.describe("Admin messages management", () => {
  4  |   test.beforeEach(async ({ page }) => {
  5  |     // Mock auth for protected route
  6  |     await page.context().addInitScript(() => {
  7  |       localStorage.setItem("admin-auth", "mock-token");
  8  |     });
  9  |     await page.goto("/messages");
> 10 |     await expect(page.getByRole("heading", { name: /message/i })).toBeVisible({ timeout: 10000 });
     |                                                                   ^ Error: expect(locator).toBeVisible() failed
  11 |   });
  12 | 
  13 |   test("messages page loads and shows heading", async ({ page }) => {
  14 |     const heading = page.getByRole("heading", { name: /message/i });
  15 |     await expect(heading).toBeVisible();
  16 |   });
  17 | 
  18 |   test("messages list or empty state is visible", async ({ page }) => {
  19 |     // Either a list/card of messages or an empty state should be shown
  20 |     const hasList = await page.locator('[class*="card"]').first().isVisible().catch(() => false);
  21 |     const hasEmpty = await page.getByText(/no messages|empty/i).isVisible().catch(() => false);
  22 |     expect(hasList || hasEmpty).toBe(true);
  23 |   });
  24 | 
  25 |   test("filter tabs are available with correct labels", async ({ page }) => {
  26 |     const allTab = page.getByRole("tab", { name: /all/i });
  27 |     const unreadTab = page.getByRole("tab", { name: /unread/i });
  28 | 
  29 |     await expect(allTab).toBeVisible({ timeout: 5000 });
  30 |     await expect(unreadTab).toBeVisible({ timeout: 5000 });
  31 | 
  32 |     // Verify tab content shows counts (e.g., "All (5)")
  33 |     const allTabText = await allTab.textContent();
  34 |     expect(allTabText).toMatch(/all/i);
  35 |   });
  36 | 
  37 |   test("can switch between filter tabs", async ({ page }) => {
  38 |     const allTab = page.getByRole("tab", { name: /all/i });
  39 |     const unreadTab = page.getByRole("tab", { name: /unread/i });
  40 | 
  41 |     await expect(unreadTab).toBeVisible({ timeout: 5000 });
  42 |     await unreadTab.click();
  43 | 
  44 |     // Tab should become selected (aria-selected or data-state)
  45 |     await expect(unreadTab).toHaveAttribute("aria-selected", "true", { timeout: 3000 });
  46 |   });
  47 | 
  48 |   test("message count summary is displayed in heading", async ({ page }) => {
  49 |     // The page heading should show total message count
  50 |     const heading = page.getByRole("heading", { name: /message/i });
  51 |     const headingText = await heading.textContent();
  52 |     // Should contain a count or just the word "Messages"
  53 |     expect(headingText).toBeTruthy();
  54 |   });
  55 | 
  56 |   test("message action buttons are present when messages exist", async ({ page }) => {
  57 |     const hasMessages = await page.locator('[class*="card"]').first().isVisible().catch(() => false);
  58 | 
  59 |     if (hasMessages) {
  60 |       // Check for action buttons on a message card
  61 |       const replyBtn = page.getByRole("button", { name: /reply/i });
  62 |       const deleteBtn = page.getByRole("button", { name: /delete/i });
  63 | 
  64 |       // At least one action button should be visible
  65 |       const hasActions = (await replyBtn.isVisible().catch(() => false)) ||
  66 |                          (await deleteBtn.isVisible().catch(() => false));
  67 |       expect(hasActions).toBe(true);
  68 |     }
  69 |   });
  70 | });
  71 | 
```